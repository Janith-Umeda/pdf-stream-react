{
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api" },
        {
            "source":"/content/:bucket/:match*",
            "destination":"https://:bucket.nyc3.cdn.digitaloceanspaces.com/uploads/:match*"
        }
    ]
}